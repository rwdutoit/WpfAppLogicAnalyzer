<Window x:Class="WpfAppLogicAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        xmlns:local="clr-namespace:WpfAppLogicAnalyzer"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"
                Orientation="Horizontal"
                Margin="10">
            <Button Content="Fetch"
                    Click="ButtonFetch_Click"
                    Margin="0,0,10,0"/>
            <TextBox x:Name="sampleAmount"
                    Text="{Binding MyValue, FallbackValue=500}"
                    Width="120"
                    Margin="0,0,10,0"/>
            <Label Content="IP Address:"
                    Margin="0,0,5,0"/>
            <TextBox x:Name="ipAddressText"
                    Text="192.168.0.101"
                    Width="120"/>
        </StackPanel>
        <ScottPlot:WpfPlot Name="WpfPlot1"
                Grid.Row="1"
                Margin="10"/>
        <TextBox x:Name="dataText"
                Grid.Row="2"
                Margin="10"
                IsReadOnly="True"
                AcceptsReturn="True"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
